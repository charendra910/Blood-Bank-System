@model IEnumerable<ReceiveBloodModel>

@{
    ViewData["Title"] = "ReceiverView";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@if (TempData["SuccessMessage1"] != null)
{
    <div class="alert alert-success text-center">
        @TempData["SuccessMessage1"]
    </div>
}

<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            background-color: white; /* Choose your desired background color */
        }

        .box {
            width: calc(33.33% - 10px);
            background-color: #f0f0f0;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 10px;
        }

        .box-two {
            width: calc(50% - 10px);
            background-color: #f0f0f0;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 10px;
        }

        .bd-color {
            background-color: antiquewhite;
        }

        .approve-btn {
            padding-left: 50px;
            padding-right: 40px;
            /* Adjust as needed */
        }

        .reject-btn {
            padding-left: 50px;
            padding-right: 50px;
        }

        .space {
            font-size: 42px;
            line-height: 42px;
            margin-bottom: 20px;
            font-weight: 700;
            color: #555555;
            letter-spacing: 0.20rem;
        }

    </style>
</head>
<body>

    <div class="container mt-2">
        <div class="row justify-content-center">
            <div class="col-6 mb-4">
                <h2 class="text-center space">RECEIVERS DETAILS</h2>
            </div>
        </div>
    </div>

    <div class="container">
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>Receiver ID</th>
                    <th>Full Name</th>
                    <th>Email</th>
                    <th>Age</th>
                    <th>Gender</th>
                    <th>Blood Type</th>
                    <th>Contact</th>
                    <th>Address</th>
                    <th>Date of Requirement</th>
                    <th>Reason for Requirement</th>
                    <th>Operation</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var receiver in Model)
                {
                    <tr>
                        <td>@receiver.ReceiverID</td>
                        <td>@receiver.FullName</td>
                        <td>@receiver.Email</td>
                        <td>@receiver.Age</td>
                        <td>@receiver.Gender</td>
                        <td>@receiver.BloodType</td>
                        <td>@receiver.ContactNumber</td>
                        <td>@receiver.Address</td>
                        <td>@receiver.DateOfRequirement.ToString("yyyy-MM-dd")</td>
                        <td>@receiver.MedicalHistory</td>
                        <td role="group">
                            <div class="mb-2">
                                <div class="col-md-12 mb-2">
                                    <a href="@Url.Action("EditReceiversData", "Receiver", new { id = receiver.ReceiverID })" class="btn btn-primary btn-sm w-100">Edit</a>
                                </div>
                                <div class="col-md-12">
                                    <form method="post" action="@Url.Action("ReceiverDelete", "Receiver", new { id = receiver.ReceiverID })">
                                        <button type="submit" class="btn btn-danger btn-sm w-100">Delete</button>
                                    </form>
                                </div>
                            </div>
                        </td>

                    </tr>
                }
            </tbody>
        </table>
    </div>

</body>
</html>
